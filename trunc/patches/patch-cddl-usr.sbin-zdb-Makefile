--- cddl/usr.sbin/zdb/Makefile.orig	2014-02-26 20:12:45.000000000 +0400
+++ cddl/usr.sbin/zdb/Makefile	2014-02-26 20:12:58.000000000 +0400
@@ -27,8 +27,4 @@
 	${LIBUUTIL} ${LIBZFS_CORE} ${LIBZFS} ${LIBZPOOL}
 LDADD=	-lgeom -lm -lnvpair -lpthread -lumem -luutil -lzfs_core -lzfs -lzpool
 
-# Since there are many asserts in this program, it makes no sense to compile
-# it without debugging.
-CFLAGS+=	-g -DDEBUG=1
-
 .include <bsd.prog.mk>
